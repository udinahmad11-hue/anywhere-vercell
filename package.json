{
  "name": "cors-anywhere-vercel",
  "version": "1.0.0",
  "description": "CORS Anywhere with Singapore IP for Vercel",
  "main": "api/[...url].js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "node-fetch": "^2.7.0"
  },
  "engines": {
    "node": "18.x"
  }
}
